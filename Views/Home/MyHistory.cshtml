@model JJAutos.Models.MyHistoryViewModel
@using Microsoft.AspNet.Identity;
@{
    ViewBag.Title = "MyHistory";
}

    @foreach (var item in Model.MyQoutes)
    {
        Html.RenderPartial("_Qoute",item);
    }
